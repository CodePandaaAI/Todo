# Android Studio / IntelliJ
# Ignores the entire .idea directory, which is the root cause of your issue.
.idea/

# Gradle
.gradle/
/build/

# Android build outputs and generated files
/captures/
.externalNativeBuild/
.cxx/

# Local configuration files that should not be committed
# Contains machine-specific paths (like SDK location)
local.properties

# Android Studio module files
*.iml

# Mac-specific file
.DS_Store

# IMPORTANT: You should almost always commit google-services.json
# Remove the line below if it's not a secret and is needed for the project to build.
# If it contains sensitive info, it's correct to ignore it, but your team needs another way to get it.
google-services.json
